name: SSH Runner

on:
  workflow_dispatch:

jobs:
  ssh:
    runs-on: ubuntu-latest

    steps:
      - name: Start ssh
        uses: csexton/debugger-action@master
        
      - name: upload
        uses: actions/upload-artifact@v4
        with:
          name: output-gguf
          path: /home/runner/output.gguf
